name: Repro setup-python OS-incompatible cache reuse

on:
  workflow_dispatch:

jobs:
  ubuntu-24-first:
    runs-on: self-hosted
    container:
      image: ubuntu:24.04
    steps:
      - name: Install tools
        run: |
          apt-get update
          DEBIAN_FRONTEND=noninteractive apt-get install -y \
            bash curl ca-certificates git jq tar xz-utils file
      - name: OS info
        run: |
          cat /etc/os-release
          echo "RUNNER_TOOL_CACHE=$RUNNER_TOOL_CACHE"

      - name: Setup Python
        uses: gowridurgad/setup-python@arch-suffix-v2
        with:
          python-version: '3.8'
          scope-cache-by-os: true

      - name: Inspect Python
        run: |
          echo "pythonLocation=$pythonLocation"
          which python || true
          python --version || true
          python -m pip --version || true
          python -c "import sys; print(sys.executable)" || true
          python -c "import ssl; print(ssl.OPENSSL_VERSION)" || true
          ldd "$(which python)" || true
      - name: Warm cache
        run: python -m pip install --upgrade pip

  ubuntu-20-second:
    needs: ubuntu-24-first
    runs-on: self-hosted
    container:
      image: ubuntu:20.04
    steps:
      - name: Install tools
        run: |
          apt-get update
          DEBIAN_FRONTEND=noninteractive apt-get install -y \
            bash curl ca-certificates git jq tar xz-utils file
      - name: OS info
        run: |
          cat /etc/os-release
          echo "RUNNER_TOOL_CACHE=$RUNNER_TOOL_CACHE"

          
      - name: Setup Python
        uses: gowridurgad/setup-python@arch-suffix-v2
        with:
          python-version: '3.8'
          scope-cache-by-os: true

      - name: Inspect Python
        run: |
          echo "pythonLocation=$pythonLocation"
          which python || true
          python --version || true
          python -m pip --version || true
          python -c "import sys; print(sys.executable)" || true
          python -c "import ssl; print(ssl.OPENSSL_VERSION)" || true
          ldd "$(which python)" || true
      - name: Trigger bug
        run: python -m pip install --upgrade pip
